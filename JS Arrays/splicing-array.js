// METODO splice para añadir, eliminar un  elemento 

const months = ["January", "Abril", "Marzo"];

// months.splice(0,0, "Diciembre");


// remover un item array 

months.splice(0, 2);
document.write(months);